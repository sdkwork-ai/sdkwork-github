version: 2
defaultProfile: cloud.production
profiles:
  cloud.development:
    deployment:
      deploymentProfile: cloud
      environment: development
      deliveryKind: container-image
      deploymentDriver: kubernetes
      managementModel: sdkwork-managed
      tenancyModel: multi-tenant
      isolationModel: shared
      networkExposure: public
      rolloutStrategy: rolling
      availabilityMode: high-availability
    install:
      layout: binary-package
    expose:
      - tls: sdkwork.com
        mode: web
        web: adaptive
        domain: github-dev.sdkwork.com
      - tls: birdcoder.com
        mode: web
        web: adaptive
        domain: github-dev.birdcoder.com
      - tls: dtupay.com
        mode: web
        web: adaptive
        domain: github-dev.dtupay.com
      - tls: sdkwork.cn
        mode: web
        web: adaptive
        domain: github-dev.sdkwork.cn
      - tls: birdcoder.cn
        mode: web
        web: adaptive
        domain: github-dev.birdcoder.cn
      - tls: dtupay.cn
        mode: web
        web: adaptive
        domain: github-dev.dtupay.cn
    packages: []
    overrides:
      topology:
        spec: specs/topology.spec.json
        profile: cloud.development
        env: etc/topology/cloud.development.env
  cloud.production:
    deployment:
      deploymentProfile: cloud
      environment: production
      deliveryKind: container-image
      deploymentDriver: kubernetes
      managementModel: sdkwork-managed
      tenancyModel: multi-tenant
      isolationModel: shared
      networkExposure: public
      rolloutStrategy: rolling
      availabilityMode: high-availability
    install:
      layout: binary-package
    expose:
      - tls: sdkwork.com
        mode: web
        web: adaptive
        domain: github.sdkwork.com
      - tls: birdcoder.com
        mode: web
        web: adaptive
        domain: github.birdcoder.com
      - tls: dtupay.com
        mode: web
        web: adaptive
        domain: github.dtupay.com
      - tls: sdkwork.cn
        mode: web
        web: adaptive
        domain: github.sdkwork.cn
      - tls: birdcoder.cn
        mode: web
        web: adaptive
        domain: github.birdcoder.cn
      - tls: dtupay.cn
        mode: web
        web: adaptive
        domain: github.dtupay.cn
    packages: []
    overrides:
      topology:
        spec: specs/topology.spec.json
        profile: cloud.production
        env: etc/topology/cloud.production.env
  standalone.development:
    deployment:
      deploymentProfile: standalone
      environment: development
      deliveryKind: host-package
      deploymentDriver: host-service
      managementModel: customer-managed
      tenancyModel: single-tenant
      isolationModel: dedicated
      networkExposure: private
      rolloutStrategy: recreate
      availabilityMode: single-instance
    install:
      layout: binary-package
    expose:
      - tls: sdkwork.com
        mode: web
        web: adaptive
        domain: github-dev.sdkwork.com
      - tls: birdcoder.com
        mode: web
        web: adaptive
        domain: github-dev.birdcoder.com
      - tls: dtupay.com
        mode: web
        web: adaptive
        domain: github-dev.dtupay.com
      - tls: sdkwork.cn
        mode: web
        web: adaptive
        domain: github-dev.sdkwork.cn
      - tls: birdcoder.cn
        mode: web
        web: adaptive
        domain: github-dev.birdcoder.cn
      - tls: dtupay.cn
        mode: web
        web: adaptive
        domain: github-dev.dtupay.cn
    packages: []
    overrides:
      topology:
        spec: specs/topology.spec.json
        profile: standalone.development
        env: etc/topology/standalone.development.env
  standalone.production:
    deployment:
      deploymentProfile: standalone
      environment: production
      deliveryKind: host-package
      deploymentDriver: host-service
      managementModel: customer-managed
      tenancyModel: single-tenant
      isolationModel: dedicated
      networkExposure: private
      rolloutStrategy: recreate
      availabilityMode: single-instance
    install:
      layout: binary-package
    expose:
      - tls: sdkwork.com
        mode: web
        web: adaptive
        domain: github.sdkwork.com
      - tls: birdcoder.com
        mode: web
        web: adaptive
        domain: github.birdcoder.com
      - tls: dtupay.com
        mode: web
        web: adaptive
        domain: github.dtupay.com
      - tls: sdkwork.cn
        mode: web
        web: adaptive
        domain: github.sdkwork.cn
      - tls: birdcoder.cn
        mode: web
        web: adaptive
        domain: github.birdcoder.cn
      - tls: dtupay.cn
        mode: web
        web: adaptive
        domain: github.dtupay.cn
    packages: []
    overrides:
      topology:
        spec: specs/topology.spec.json
        profile: standalone.production
        env: etc/topology/standalone.production.env
